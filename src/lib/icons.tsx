import React from 'react';
import { Wrench, LucideProps } from 'lucide-react';

// Create a Tool icon component that renders the Wrench icon
export const Tool = (props: LucideProps) => {
  return <Wrench {...props} />;
};

// Export other icons as needed from here
